<Window x:Class="PKS_sem4_kr3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTP Monitor" Height="750" Width="1100"
        Background="#1E1E2E"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Стиль для кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B4BEFE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#45475A"/>
                                <Setter Property="Foreground" Value="#6C7086"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для текстовых полей -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,3"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="5"
                                                Padding="8,5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" 
                                                                Content="{TemplateBinding ContentControl.Content}"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Margin="0,0,5,0"/>
                                                <Path x:Name="Arrow" 
                                                    Grid.Column="1"
                                                    Fill="#1E1E2E"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Border Background="#313244" 
                                        BorderBrush="#45475A" 
                                        BorderThickness="1" 
                                        CornerRadius="5"
                                        Margin="0,2"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для элементов ComboBox -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#89B4FA"/>
                                <Setter Property="Foreground" Value="#1E1E2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#181825"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter Margin="5"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="Transparent"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Background="#1E1E2E" 
                                        HorizontalAlignment="Left" 
                                        Margin="15,-12,0,0"
                                        Padding="8,2">
                                    <TextBlock Text="{TemplateBinding Header}" 
                                               Foreground="#89B4FA"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="0,10,0,0"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Разделитель -->
        <Border Grid.Column="1" Background="#45475A" Width="2" Margin="0,10"/>
        
        <!-- ==================== ЛЕВАЯ ЧАСТЬ: СЕРВЕР ==================== -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок -->
            <Border Background="#313244" CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel>
                        <TextBlock Text="🌐 HTTP SERVER" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#89B4FA"/>
                        <TextBlock Text="HttpListener • Многопоточная обработка" 
                                   FontSize="11" 
                                   Foreground="#6C7086"
                                   Margin="0,3,0,0"/>
                    </StackPanel>
                    
                    <Border Grid.Column="1" Background="#181825" CornerRadius="5" Padding="10">
                        <StackPanel>
                            <TextBlock Text="СТАТИСТИКА" 
                                       FontSize="10" 
                                       Foreground="#6C7086"
                                       Margin="0,0,0,5"/>
                            <WrapPanel>
                                <Border Background="#313244" CornerRadius="3" Padding="8,3" Margin="0,0,5,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="GET:" Foreground="#A6E3A1" Margin="0,0,5,0"/>
                                        <TextBlock x:Name="txtGetCount" Text="0" Foreground="#CDD6F4" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#313244" CornerRadius="3" Padding="8,3" Margin="0,0,5,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="POST:" Foreground="#FAB387" Margin="0,0,5,0"/>
                                        <TextBlock x:Name="txtPostCount" Text="0" Foreground="#CDD6F4" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#313244" CornerRadius="3" Padding="8,3">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Avg:" Foreground="#CBA6F7" Margin="0,0,5,0"/>
                                        <TextBlock x:Name="txtAvgTime" Text="0ms" Foreground="#CDD6F4" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Управление сервером -->
            <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="PORT:" 
                                Foreground="#6C7086" 
                                VerticalAlignment="Center" 
                                Margin="0,0,10,0"/>
                        <TextBox x:Name="txtPort" 
                                Text="8080" 
                                Width="60" 
                                TextAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnStart" 
                                Content="▶ START" 
                                Click="btnStart_Click"/>
                        <Button x:Name="btnStop" 
                                Content="■ STOP" 
                                IsEnabled="False" 
                                Click="btnStop_Click"
                                Background="#F38BA8"/>
                        
                        <!-- ИСПРАВЛЕННЫЙ БЛОК СТАТУСА -->
                        <Border Background="#45475A" CornerRadius="5" Padding="10,5" Margin="15,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="statusIndicator" Width="10" Height="10" Fill="#F38BA8" Margin="0,0,8,0"/>
                                <TextBlock x:Name="txtStatus" 
                                        Text="STOPPED" 
                                        Foreground="#CDD6F4" 
                                        FontWeight="Bold"
                                        FontSize="11"
                                        VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Логи -->
            <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="📋 LOGS" 
                                   FontSize="14" 
                                   FontWeight="Bold" 
                                   Foreground="#89B4FA"/>
                        <TextBlock Text="(" Foreground="#6C7086" Margin="5,0,0,0"/>
                        <TextBlock x:Name="txtLogCount" Text="0" Foreground="#A6E3A1"/>
                        <TextBlock Text=")" Foreground="#6C7086"/>
                    </StackPanel>
                    
                    <ListBox x:Name="lstLogs" Grid.Row="1"/>
                </Grid>
            </Border>
        </Grid>
        
        <!-- ==================== ПРАВАЯ ЧАСТЬ: КЛИЕНТ ==================== -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок -->
            <Border Background="#313244" CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📡 HTTP CLIENT" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#89B4FA"/>
                    <TextBlock Text="HttpClient • Поддержка GET/POST" 
                               FontSize="11" 
                               Foreground="#6C7086"
                               Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Клиент -->
            <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- URL и метод -->
                    <StackPanel>
                        <TextBlock Text="URL" Foreground="#6C7086" FontSize="11" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtUrl" 
                                     Text="http://localhost:8080/"/>
                            <ComboBox x:Name="cmbMethod" 
                                    Grid.Column="1" 
                                    Width="80" 
                                    SelectedIndex="0"
                                    Margin="5,0">
                                <ComboBoxItem Content="GET"/>
                                <ComboBoxItem Content="POST"/>
                            </ComboBox>
                            <Button Grid.Column="2" 
                                    Content="➤ SEND" 
                                    Click="btnSend_Click"
                                    Background="#A6E3A1"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Тело запроса -->
                    <GroupBox Grid.Row="1" Header="REQUEST BODY" Margin="0,15,0,10">
                        <TextBox x:Name="txtBody" 
                                 Height="80"
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </GroupBox>
                    
                    <!-- Разделитель -->
                    <Border Grid.Row="2" Height="1" Background="#45475A" Margin="0,5"/>
                    
                    <!-- Заголовок ответа -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="📥 RESPONSE" 
                                   FontSize="12" 
                                   FontWeight="Bold" 
                                   Foreground="#89B4FA"/>
                        <TextBlock x:Name="txtResponseStatus" 
                                   Text="" 
                                   Foreground="#6C7086" 
                                   Margin="10,0,0,0"
                                   FontSize="11"/>
                    </StackPanel>
                    
                    <!-- Ответ -->
                    <TextBox x:Name="txtResponse" 
                             Grid.Row="4" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             Background="#181825"/>
                </Grid>
            </Border>
            
            <!-- Статистика клиента -->
            <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="📊 Requests:" Foreground="#6C7086" Margin="0,0,10,0"/>
                    <TextBlock x:Name="txtTotalRequests" Text="0" Foreground="#A6E3A1" FontWeight="Bold" Margin="0,0,20,0"/>
                    <TextBlock Text="⚡ Avg Time:" Foreground="#6C7086" Margin="0,0,10,0"/>
                    <TextBlock x:Name="txtClientAvgTime" Text="0ms" Foreground="#FAB387" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>